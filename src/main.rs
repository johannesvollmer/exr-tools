mod extract_meta;
mod edit_meta;
mod compression_stats;

fn main(){
    // todo:
    //    - parse first env argument (ex. "extract-meta" in "exr-tools extract-meta x y z")
    //    - match argument and call the corresponding module main ("extract_meta::main()")
}